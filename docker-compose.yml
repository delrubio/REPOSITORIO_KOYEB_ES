version: "3.8"

services:
  nginx:
    image: mi-nginx:1
    container_name: nginx-compose
    ports:
      - "8080:8080"
    volumes:
      - ./conf/nginx/nginx.conf:/etc/nginx/nginx.conf        # SOLO nginx.conf
      - ./data/nginx/html:/usr/share/nginx/html              # tus webs estáticas